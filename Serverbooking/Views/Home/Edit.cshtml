@model Serverbooking.Models.InfoServer

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>
</head>
<body>
    <div>
        <h4>InfoServer</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Status)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Status)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ServerName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ServerName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Environment)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Environment)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.ActiveBookingID)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ActiveBookingID)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.ActiveBookingID)
            </dt>
            <dd>
                <table>
                    <tr>
                        <th>ServerID</th>
                        <th>Status</th>
                    </tr>
                    @foreach (var item in Model.ServerName)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.ServerID)
                            </td>
                            <td>
                                @Html.DisplayNameFor(modelItem => item.Status)
                            </td>
                        </tr>
                    }
                </table>
            </dd>
        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.ServerID }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
</body>
</html>
